import { CommoninsideDirective } from './commoninside.directive';

describe('CommoninsideDirective', () => {
  it('should create an instance', () => {
    const directive = new CommoninsideDirective();
    expect(directive).toBeTruthy();
  });
});
